Définition : SpringBoot a pour but de faciliter la mise en place d'un projet avec le frameWork Spring:
1) les starters : sont en fait des méga dépendances qui regroupent les dépendances dont on a besoin pour lancer un projet.
Exemple besoin de faire une appli web , donc d'avoir des dépendances liées au web, il suffit d'utiliser le starter "Spring  starter web".

2) Pour simplifier le démarage d'un projet et partir avec toutes les dépendances dont on a besoin, il suffit d'ajouter les annotations: 
@EnableAutoConfiguration ou @SpringBootApplication.

3) app autonome packagé comme un jar avec un serveur tomcat intégré. 

4) IHM est l'acronyme d'Interface Homme-Machine, qui se réfère à la partie d'un système informatique qui permet à un utilisateur d'interagir avec le logiciel ou l'application. Angular, quant à lui, est un framework JavaScript populaire pour la création d'applications web.

Ainsi, une IHM Angular est une interface utilisateur créée à l'aide du framework Angular.
Cela implique l'utilisation des composants et des directives Angular pour construire l'interface utilisateur 
et la rendre dynamique en fonction de l'interaction de l'utilisateur avec l'application. 
En d'autres termes, c'est une interface utilisateur créée à l'aide de la technologie Angular.

INITIALISER un projet springBoot:
1) https://start.spring.io/
2) choisir : project Maven , java  Springboot version stable.
dans "Project Metadata : changer le nom du group, de l'artifact, description, et choisir version de java et packing avec jar.
Dans les dépendaces: choisir pour un projet web, la dépendances "springWeb".
puis cliquer sur "generate".
